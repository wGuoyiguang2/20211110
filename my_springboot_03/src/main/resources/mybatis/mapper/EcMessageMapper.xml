<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.EcMessageMapper">


    <select id="getCountByTimeAndTitle" resultType="java.lang.Integer">
        select count(1)  from ec_message where  pod = #{pod} and   title = #{title}  and starts_at = #{startsAt}  and ends_at =  #{endsAt} and is_deleted = 0






    </select>

</mapper>
